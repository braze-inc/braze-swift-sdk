import Foundation

enum BannerUI {

  enum Constants {
    static let fullBannerPlacementID = "sdk-test-1"
    static let wideBannerPlacementID = "sdk-test-2"

    /// Estimated maximum height (in points) for the test banner used in this example.
    /// Caps the banner height to prevent it from taking over the screen on larger devices.
    static let wideBannerMaxHeight: CGFloat = 410
  }

}
